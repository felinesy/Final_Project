package object;

import Main.GamePanel;
import entity.Entity;

public class Obj_RedPotion extends Entity {
    public Obj_RedPotion(GamePanel gp){
        super(gp);

        name = "Mana Potion";
        stackable = true;
        image = setup("/objects/red_glass_jar", gp.tileSize, gp.tileSize);
        description = "Gains you +15 mana.";
    }
}
